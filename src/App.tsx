import * as React from 'react';

import { Game } from './components/Game';

export class App extends React.Component {
    render() {
        return (
            <div>
                <Game />
            </div>
        );
    }
}